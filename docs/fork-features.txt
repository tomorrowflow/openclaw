# Fork feature checklist — one grep-verifiable entry per line.
# Format: pattern | file | description
# Used by step 5f in UPSTREAM-SYNC.md to verify fork features survived rebase.
# Update this file when adding or removing fork features.

# --- TTS / Audio ---
# RETIRED: bare [[tts]] detection is upstream's classifyTtsTag in src/tts/directives.ts
# (normalized === "tts"). The literal "[[tts]]" was absent from the fork's own copy too,
# so this entry reported missing on every run and taught readers to ignore the check.
kokoroTTS | src/tts/providers/kokoro.ts | Kokoro TTS provider implementation
buildKokoroSpeechProvider | src/tts/provider-registry.ts | Kokoro registered as built-in speech provider

# --- Speech-to-Text (Whisper ASR) ---
whisperAsrProvider | src/media-understanding/provider-registry.ts | Whisper ASR registered in provider list
whisper-asr | src/media-understanding/providers/whisper-asr/index.ts | Whisper ASR native provider module
transcribeWhisperAsr | src/media-understanding/providers/whisper-asr/audio.ts | Whisper ASR transcription implementation
"faster-whisper" | src/media-understanding/providers/whisper-asr/audio.ts | Whisper ASR default model. 2026.9.3 dissolved the central DEFAULT_AUDIO_MODELS map (upstream keeps per-provider defaults in the providers), so this now asserts the provider fallback that actually resolves the model.
LOCAL_KEYLESS_PROVIDERS | src/media-understanding/runner.entries.ts | Whisper ASR keyless provider bypass
DEFAULT_FFMPEG_API_URL | src/media-understanding/providers/whisper-asr/audio.ts | FFmpeg API URL default (localhost:9008)

# --- Streaming / Tag Stripping ---
stripReasoningTagsFromText | src/shared/text/reasoning-tags.ts | Reasoning tag stripping helper
stripReasoningTags | src/tui/tui-formatters.ts | TUI reasoning tag stripping
stripReasoningTagsFromText\(rawFinalText | src/gateway/server-chat.ts | Gateway reasoning tag stripping on the final chat broadcast. Matches the call, not the import: the 2026.9.5 re-cut dropped the call while the helper stayed importable elsewhere.
splitTrailingBlockTagFragment | src/agents/embedded-agent-subscribe.stream-rendering.ts | Streaming partial tag stripping (upstreamed 2026.8.1; the fork's stripTrailingPartialTag DISCARDED a trailing "<..." fragment, upstream buffers it as pendingTagFragment and re-emits it with the next chunk — strictly better, and code-span aware. Do not re-add the fork helper.)
# RETIRED: the fork helper is gone from that file (and was already gone pre-rebase).
# Partial-tag handling is upstream's splitTrailingBlockTagFragment, checked above.
closedOnly | packages/markdown-core/src/code-spans.ts | Code-span closedOnly option (partial tag support)

# --- Sandbox ---
mapSandboxSkillEntriesForPrompt | src/agents/embedded-agent-runner/sandbox-skills.ts | Sandbox skill prompt rebuild from in-container paths (upstreamed 2026.7.2; fixes host-path SKILL.md leak incl. rw sandboxes)
mapSandboxSkillEntriesForPrompt\( | src/agents/embedded-agent-runner/skill-runtime.ts | Sandbox skill prompt rebuild wired into the skill runtime (upstreamed 2026.7.2; 2026.9.3 moved the call here from run/attempt-setup.ts). Matches the call, not the import.
mapSandboxSkillEntriesForPrompt\( | src/auto-reply/reply/commands-system-prompt.ts | Sandbox skill prompt rebuild wired into the command system prompt (upstreamed 2026.7.2; 2026.9.3 moved the call here from prepared-compaction-runtime.ts). Matches the call, not the import.
# Private-network navigation in the sandbox browser is NO LONGER a code patch.
# Upstream 2026.7.2 plumbs browser.ssrfPolicy from config into the sandbox bridge
# (context.ts -> ensureSandboxBrowser -> buildSandboxBrowserResolvedConfig), and
# core stays fail-closed by design. The fork enables it in the operator's
# openclaw.json instead: browser.ssrfPolicy.dangerouslyAllowPrivateNetwork: true.
# Nothing to grep for in the repo — verify on the deployed install, not here.
ssrfPolicy | src/agents/sandbox/browser.ts | Sandbox bridge accepts an operator SSRF policy (the seam the fork's private-network access depends on — if this plumbing disappears, config can no longer reach the sandbox browser)
containerPath:[[:space:]]SANDBOX_MEDIA_MOUNT | src/agents/sandbox/workspace-mounts.ts | Media dir mount (STATE_DIR/media -> /media, read-only). Asserts the mounts.push, not the constant: the 2026.9.2 rebase kept the constants in constants.ts and dropped both push sites, and an entry pointing at the declaration still matched while agents got no mount. 2026.9.5 moved the table into resolveWorkspaceMounts(), the one selection that feeds both container creation and the file tools, so the push lives there now.
containerPath:[[:space:]]SANDBOX_SHARED_MOUNT | src/agents/sandbox/workspace-mounts.ts | Shared dir mount (STATE_DIR/shared -> /workspace/shared). Asserts the mounts.push, not the constant — see the media entry above.
resolveSandboxHostPathForContainerPath | src/agents/sandbox/fs-paths.ts | Container-path -> host-path translation through the mount selection, so read surfaces outside the container can serve a file the agent named as /workspace/shared/... . The shared mount is nested under /workspace and does not live under the workspace root, so stripping the container prefix resolves a different directory.
resolveSandboxContainerFilePath\( | src/gateway/server-methods/workspace-files.ts | Container paths accepted as a session-file candidate for preview, browsing, and save. Asserts the call inside resolveSessionFileCandidates, not the import: without it the Control UI renders a file link for a shared path and the click fails.
await resolveSessionSandboxPaths\( | src/gateway/server-methods/sessions-files.ts | The container->host mapping the file surface translates with, resolved on the read and write paths. Asserts the calls, not the helper — an uncalled resolver leaves the translator importable and permanently inactive, the same dead-feature shape as the dropped mount pushes above.
mounts: mountPlan.mounts | src/agents/sandbox/docker.ts | Container creation records its Gateway-local mount table in the sandbox registry. Without it the file surface falls back to deriving the mapping from current config, which can describe a container that has not been recreated yet and so resolve a real but wrong file.
assistant.silent \? "optional" | src/agents/embedded-agent-runner/run/incomplete-turn-recovery.ts | An authored NO_REPLY completes the turn even when the host asked for a required reply. 2026.9.6's resolveReplyExpectation makes an explicit terminalReplyExpectation win outright, so a cron or heartbeat run that requests delivery (required) plus allowEmptyAssistantReplyAsSilent reads authored silence as a missing answer and reopens the turn — the "couldn't generate a response" leak. Asserts the expectation override, not the import: without it the guard compiles and the leak returns.
bridgeOwnerLive | src/agents/sandbox/browser.ts | Cached browser bridges are only reused while the plugin generation that started them still accepts calls. Without it a plugins.* config reload strands every sandbox provisioning on a retired instance ("Plugin browser was reloaded or disabled") until the Gateway restarts.
SANDBOX_BROWSER_HOME_MOUNT\} | src/agents/sandbox/browser.ts | Persistent browser home dir mount. Matches the docker -v argument rather than the const declaration, so losing the bind is detected.
validateSecretMounts | src/agents/sandbox/validate-sandbox-security.ts | Secret mounts security validation
secretMounts | src/config/zod-schema.sandbox.ts | Secret mounts config schema. 2026.9.5 moved the sandbox schemas to their own module and agent-runtime now imports SandboxDockerSchema from there, so the field and its key/path validation live with the canonical schema.
secretMounts | src/agents/sandbox/config.ts | Secret mounts config merge logic
secretMounts | src/agents/sandbox/docker.ts | Secret mounts injected as env vars in container

# --- Cron Hardening ---
MAX_JOBS_PER_AGENT | src/cron/service/ops-mutations.ts | Per-agent cron job count limit
configuredTimeoutMs <= 0 | src/cron/service/timeout-policy.ts | Timeout hardening (<=0 falls back to default)
createCronScheduleSchema | src/agents/tools/cron-tool-schema.ts | Explicit TypeBox schema for cron job/patch objects (upstreamed 2026.7.2; schema split out of cron-tool.ts)

# --- Auth ---
ownerAllowAll | src/auto-reply/command-auth.ts | Wildcard (*) support for ownerAllowFrom

# --- CLI ---
enablePositionalOptions | src/cli/program/build-program.ts | Positional options fix (prevents option shadowing)
enablePositionalOptions | src/cli/gateway-cli/register.ts | Positional options fix for gateway register
deviceId | src/cli/devices-cli.ts | Device ID display in devices list

# --- ESM resolve fast path (deploy-layout compat) ---
parentUrl\.includes\("/node_modules/"\) | src/entry.esm-resolve-fast-path.ts | Dist ESM resolve fast path skips vendored node_modules. scripts/deploy-globally.mjs installs each externalized extension's dependencies under dist/extensions/*/node_modules, which breaks the hook's assumption that everything below the dist root is ESM: it forced format "module" onto CommonJS (tslib.js), so memory-lancedb died at load with "'../tslib.js' does not provide an export named 'default'" and the memory slot lost its provider. Upstream's own layout has no node_modules under dist, so this only bites the fork's deploy.

# --- Ollama (provider compat patches) ---
ensureArgsString | extensions/ollama/src/stream.runtime.ts | OpenAI-compat tool_call arguments kept as string (fixes ollama 400 "cannot unmarshal object ... arguments of type string" on tool-call replay; e.g. glm-5.2:cloud)

# --- Deploy (root package.json fork scripts) ---
# Dropped whenever the rebase takes upstream's root package.json wholesale; the
# script FILE survives but becomes uninvokable. See sync-prompt Step 5g.
deploy:globally | package.json | Fork deploy script (node scripts/deploy-globally.mjs) — bundles externalized plugins into the global install

# --- Memory (LanceDB) ---
storageOptions | extensions/memory-lancedb/config.ts | LanceDB storageOptions config
nomic-embed-text | extensions/memory-lancedb/config.ts | Nomic embedding model dimension mapping
baseUrl | extensions/memory-lancedb/config.ts | Embedding API baseUrl config

# --- Skills ---
Kokoro TTS | skills/kokoro-tts/SKILL.md | Kokoro TTS skill (agent instructions)
whisper-asr | skills/whisper-asr/SKILL.md | Whisper ASR skill (agent instructions)
excalidraw | skills/excalidraw/SKILL.md | Excalidraw skill (agent instructions)

# --- Agents / MCP ---
retireSupersededSessionRuntime | src/agents/agent-bundle-mcp-manager.ts | Retires the MCP runtime a session key rolls off. Isolated heartbeat runs roll a key to a fresh session id without a reset and idle sweeping is opt-in, so without this every superseded runtime kept its server processes alive (bundle MCP process leak). Diverges from upstream's run-settlement expectation that a non-run-owned successor survives; see the comment in run-settlement.test.ts.

# --- Plugins / Skills / Dockerfiles ---
excalidraw | extensions/excalidraw/openclaw.plugin.json | Excalidraw canvas plugin
denoland/deno | scripts/docker/sandbox/Dockerfile | Deno runtime installed in sandbox base image (official release binary on PATH)
ffmpeg | scripts/docker/sandbox/Dockerfile | ffmpeg installed in the sandbox base image so agents can convert images and media in-container
imagemagick | scripts/docker/sandbox/Dockerfile | ImageMagick in the sandbox base image for still-image work ffmpeg does not cover (resize/crop/composite, EXIF, ICO/SVG)
poppler-utils | scripts/docker/sandbox/Dockerfile | poppler-utils in the sandbox base image so agents can read PDFs (pdftotext/pdftoppm); /workspace/shared holds PDFs
gh | scripts/docker/sandbox/Dockerfile | GitHub CLI in the sandbox base image. The GH_TOKEN secret mount is configured for it, so losing this leaves a provisioned credential with no consumer
pandoc | scripts/docker/sandbox/Dockerfile | pandoc in the sandbox base image for document conversion (docx/odt/html/md)
fdfind | scripts/docker/sandbox/Dockerfile | fd/bat conventional-name symlinks over Debian's fdfind/batcat, so agent commands resolve
curl_cffi | scripts/docker/sandbox/Dockerfile | curl_cffi installed into system Python (PEP 668 break-system-packages) for sandbox scripts
brave-browser | Dockerfile.sandbox-browser-brave | Brave browser overlay Dockerfile
fonts-noto-cjk | scripts/docker/sandbox/Dockerfile.browser | CJK/Unicode fonts in sandbox browser image
1920x1080 | scripts/sandbox-browser-entrypoint.sh | Default screen resolution bump
SingletonLock | scripts/sandbox-browser-entrypoint.sh | Clear stale Chrome locks on startup
X11-unix | scripts/sandbox-browser-entrypoint.sh | Recreate X11-unix dir for tmpfs resilience
# Presence check only — never pin a version literal here. This entry exists because
# the release-rebase restore step drops the dep from root package.json entirely. The
# version itself must always TAKE UPSTREAM's value: 2026.7.2 needs 0.5.0 for
# configureFsSafeNative, and carrying the old 0.4.4 pin forward breaks every build.
@openclaw/fs-safe | package.json | Upstream dep pin the rebase keeps dropping from root package.json (take upstream's version, never the fork's)
question-gateway-runtime | package.json | Upstream plugin-sdk export the rebase keeps dropping — signal plugin fails to register with ERR_PACKAGE_PATH_NOT_EXPORTED without it
